
{% if rows %}
    <div class="container">
        <div class="row">
            <div class="col-md-10 sec-auto-margin">
                {{ header }}
                <div class="col-md-8 col-xs-12 sec-auto-margin">  
                    {% for row in rows %}
                        {{ row }}
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>
{% endif %}
